name: localboost_shared
description: Shared code for LocalBoost client and merchant apps
version: 1.0.0+1
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  dio: ^5.4.0
  shared_preferences: ^2.2.0
  provider: ^6.1.0
  flutter_local_notifications: ^17.0.0
  latlong2: ^0.9.0
  share_plus: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
